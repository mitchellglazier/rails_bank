<%= button_to "log out", destroy_user_session_path, method: :delete %>
<h1><%= current_user.first_name %>'s Accounts</h1>

<!-- user_signed_in?  checks to see if the user is logged off.
     user_session     access t the current user session. -->

<%= link_to 'Add Account', new_account_path %>
<h3>All Accounts</h3>

<ul>
  <% @accounts.each do |account| %>
  <li>
    <%= account.name %> - <%= account.balance %>
  </li>
  <% end %>
</ul>
